# K4.5 — DC-SoC Leave → Rejoin

## Environment

- Date: 2026-08-23 (Asia/Kuala_Lumpur)
- Project root: `/Users/wwiras/Documents/src/AHBN_GKEProj/ahbn2_gke`
- Python interpreter: `/Users/wwiras/Documents/src/AHBN_GKEProj/venv_ahbn2/bin/python`
- Git commit: `c3632506bb7266e1dc0d9e53f8816cb2579aca71`
- kubectl context: `gke_stoked-cosine-415611_us-central1-a_bcgossip-cluster`
- namespace: `ahbn-k4`
- active strategy: `dcsoc`
- authoritative leave: Exp11 pod deletion followed by `ApplyDCSOCMaintenance(available=false, reason=churn_leave)`
- authoritative rejoin: wait for recreated pod, replay leave to it, then broadcast `available=true, reason=churn_rejoin`

## GKE prerequisite

Command:

```bash
kubectl config current-context
kubectl get nodes -o wide
kubectl get namespaces
kubectl get pods -A -o wide
```

Initial sandboxed output (preserved):

```text
gke_stoked-cosine-415611_us-central1-a_bcgossip-cluster
Unable to connect to the server: dial tcp 35.255.43.164:443: connect: operation not permitted
```

Approved rerun output:

```text
Seven gke-bcgossip-cluster nodes: Ready, v1.35.6-gke.1641000
namespace ahbn-k4: Active
peer-0 through peer-7: 1/1 Running
```

Result: **PASS**. Existing GKE was used; no cluster was provisioned.

## Frozen churn mechanism verification

Commands:

```bash
rg -n -i "exp11|leave|rejoin|churn" app experiments tests docs
sed -n '325,375p' app/controller.py
sed -n '1,260p' app/dcsoc_maintenance.py
```

Evidence:

```text
app/controller.py run_churn:
delete_peer_pod(target, namespace)
broadcast ... available=False, reason="churn_leave"
wait_for_peer_ready(...)
broadcast ... available=False, include_affected=True, reason="churn_leave_replay"
broadcast ... available=True, reason="churn_rejoin"

app/dcsoc_maintenance.py:
ACTIVE->INACTIVE calls _leave; INACTIVE->ACTIVE calls _rejoin.
TAIL leave detaches only the edge. CORE leave elects active same-cluster
highest-degree/lowest-ID replacement. Former CORE rejoins as leaf in its
previous cluster when an active CORE remains.
```

Conclusion: pod deletion alone is not the logical leave or rejoin. The authoritative sequence above was used.

## Preflight Kubernetes state

Commands:

```bash
kubectl get namespaces
kubectl get pods -A -o wide
kubectl get svc -A
kubectl get deployments -A
kubectl get statefulsets -A
kubectl get jobs -A
kubectl -n ahbn-k4 get pods -o wide
kubectl -n ahbn-k4 get svc
kubectl -n ahbn-k4 get deployments
kubectl -n ahbn-k4 get statefulsets
kubectl -n ahbn-k4 get jobs
kubectl -n ahbn-k4 get configmap ahbn-topology -o jsonpath='{.data.topology\.json}'
```

Output:

```text
ahbn-k4/peer StatefulSet: 8/8 Ready
ahbn-k4/ahbn-peer: headless ClusterIP, 50051/TCP
deployments: none in ahbn-k4
jobs: No resources found in ahbn-k4 namespace.
strategy=dcsoc, master_id=0, fanout=3 (structural forwarding is unbudgeted)
edges: 0->1,2,3,4 and 4->5,6,7
```

One diagnostic command failed because zsh expanded brackets; preserved:

```text
zsh:1: no matches found: custom-columns=...containerStatuses[0]...
```

The quoted rerun succeeded and captured all pod UIDs.

## Initial-state anomaly and correction

The live pods retained K4.4 logical state: surviving replicas had peer-4 inactive and peer-5 CORE, while peer-4's recreated pod had static state. A probe showed peer-5 forwarding to peer-4 and peer-4 logging `dropped_failed_node`. Logical rejoin did not clear `PeerState.failed`.

Preserved failed-attempt evidence:

```text
peer-0 prior event: failed_node=4 failed_role=core replacement_core=5 reason=ch_failure
peer-5: forward_rejected dst_peer=4
peer-4: dropped_failed_node
recreated peer-7 replay: former_parent=4, while survivors reported former_parent=5
```

Smallest fixture correction:

```bash
kubectl -n ahbn-k4 rollout restart statefulset/peer
kubectl -n ahbn-k4 rollout status statefulset/peer --timeout=300s
```

Output:

```text
statefulset.apps/peer restarted
partitioned roll out complete: 8 new pods have been updated
GetStatus peers 0..7: ready=true, alive=true
COREs: peer-0 and peer-4
jobs: none
```

No manifest, image, algorithm, or cluster was changed. All subsequent case evidence is from this clean baseline.

## Initial DC-SoC state

```text
ACTIVE SET: peer-0..peer-7
MASTER/CORE: peer-0, cluster 0
CORE: peer-4, cluster 1
TAIL: peer-1,2,3 under peer-0; peer-5,6,7 under peer-4
unrelated branch: peer-0 -> peer-1,2,3
maintenance counters: core_replacement=0, recluster=0, rejoin_assignment=0
maintenance idle; no K4.5 event yet
```

# Case A — TAIL Leave → Rejoin

## Target and pre-leave dissemination

```text
target: peer-7; role=leaf/TAIL; cluster=1; parent CORE=peer-4
pod UID before leave: 64488f34-ddb2-41e9-bd14-fe38a6ba46f2
message: k4_5_tail_pre_leave_20260823T2100
path: peer-0 -> peer-4 -> peer-7
result: PASS
```

## Leave injection and detection

Commands:

```bash
kubectl -n ahbn-k4 delete pod peer-7
# broadcast node_id=7 available=false reason=churn_leave to survivors
kubectl -n ahbn-k4 wait --for=condition=Ready pod/peer-7 --timeout=180s
# broadcast node_id=7 available=false reason=churn_leave_replay to all
```

Output:

```text
2026-08-23T12:19:32Z pod "peer-7" deleted
new pod UID: 77f7306b-16ac-4e10-9a8a-07c86f434109; Running/Ready
survivors: maintenance applied
replay: survivors "no transition"; peer-7 "maintenance applied"
peer-0/peer-4: failed_node=7 failed_role=leaf former_parent=4 replacement_core=null
peer-7: maintenance_action=leave reason=churn_leave_replay former_parent=4
```

Active set after leave: `peer-0..peer-6`; peer-7 inactive. Edge `peer-4 -> peer-7` absent. Historical cluster metadata retained.

## No forwarding while inactive

```text
message: k4_5_tail_while_inactive_20260823T2100
observed: peer-0 -> peer-4; peer-4 -> peer-5,peer-6
peer-7 logs for message: none
forward decisions dst_peer=7: 0
pod state during proof: Running/Ready
result: PASS
```

## Rejoin, assignment, and post-rejoin delivery

```bash
# broadcast node_id=7 available=true reason=churn_rejoin to peers 0..7
```

Output:

```text
all replicas: maintenance applied
maintenance_action=rejoin rejoined_node=7 previous_cluster=1 new_cluster=1
previous cluster valid: yes (CORE peer-4 active)
expected/observed role: TAIL; expected/observed parent: peer-4
message k4_5_tail_post_rejoin_20260823T2100:
peer-0 -> peer-4 -> peer-7; peer-7 received_new hop=2
```

Counters (peer-0 authoritative representative):

```text
counter                    before  after-leave  after-rejoin  delta
core_replacement_count     0       0            0             0
recluster_count            0       0            0             0
rejoin_assignment_count    0       0            1             +1
leave events               0       1            1             +1
```

TAIL case: **PASS**.

# Case B — CORE Leave → Rejoin

## Target and baseline

```text
target: peer-4; role=CORE; cluster=1; parent=peer-0
downstream: peer-5,6,7
replacement candidates: peer-5,6,7 (degree 3 each)
expected replacement: peer-5 (lowest-ID tie break)
UID before leave: b473987b-4f7d-4769-a023-666174f5244d
message k4_5_core_pre_leave_20260823T2102 path: peer-0 -> peer-4 -> peer-5,6,7
```

## CORE leave and repair

Commands used the same authoritative delete/broadcast/wait/replay sequence with `node_id=4`.

```text
pod peer-4 deleted
new UID: 2ab7cfe1-431b-4555-aeae-a71f3b41ae11; Running/Ready
maintenance_action=leave reason=churn_leave
failed_node=4 failed_role=core former_parent=0 replacement_core=5
replayed replica reports the same replacement_core=5
core_replacement_count: 0 -> 1
recluster_count remains 0
```

Repaired structure: `peer-0 -> peer-5 -> peer-6,peer-7`; inactive peer-4 removed. Unrelated `peer-0 -> peer-1,2,3` branch unchanged.

## No forwarding while inactive

```text
message: k4_5_core_while_inactive_20260823T2103
path: peer-0 -> peer-5 -> peer-6,peer-7
peer-4 logs: none
forward decisions dst_peer=4: 0
result: PASS
```

## CORE rejoin and final structure

```text
all replicas: maintenance applied for available=true reason=churn_rejoin
rejoined_node=4 previous_cluster=1 new_cluster=1
expected role: TAIL (replacement peer-5 remains authoritative)
observed role: TAIL; is_cluster_head=false; parent peer-5
duplicate CORE responsibility: absent
message k4_5_core_post_rejoin_20260823T2103:
peer-0 -> peer-5 -> peer-4,peer-6,peer-7
peer-4 received_new hop=2 from peer-5
result: PASS
```

Counters (peer-0; cumulative across the clean TAIL then CORE cases):

```text
counter                    before CORE  after leave  after rejoin  CORE delta
core_replacement_count     0            1            1             +1
recluster_count            0            0            0              0
rejoin_assignment_count    1            1            2             +1
leave events               1            2            2             +1
```

CORE case: **PASS**.

## Isolation and regression

Command:

```bash
/Users/wwiras/Documents/src/AHBN_GKEProj/venv_ahbn2/bin/python \
  -m unittest tests.test_k3_5_dcsoc_dynamic tests.test_k3_6_cross_baseline
```

Output:

```text
.............................
Ran 29 tests in 0.011s
OK
```

```text
Gossip influence: NONE (strategy=dcsoc; structural target list used)
native Structured influence: NONE
AHBN influence: NONE (no AHBNController state/decision in maintenance; isolation tests pass)
self forwarding: absent in all captured paths
legacy fanout=3 truncation: absent; CORE obligations included all three children after reconstruction
```

## Final dissemination metrics and Kubernetes state

Existing metrics were emitted in `received_new`/`forward` events: hop, latency_ms, duplicates/seen state, and forwarding destinations. Final GetStatus:

```text
all peers ready=true alive=true
peer-0 is_cluster_head=true; peer-5 is_cluster_head=true; peer-4=false
peer-4 seen_count=1 (post-rejoin message)
all pods Running/Ready, restarts=0
jobs: No resources found in ahbn-k4 namespace.
```

## K4.5 PASS/FAIL checklist

```text
GKE prerequisite: PASS
authoritative leave/rejoin established and used: PASS
leave detected / active-set removal: PASS
no forwarding to inactive peer: PASS (TAIL and CORE)
rejoin detected / active-set restoration: PASS
cluster assignment and relationships valid: PASS
post-rejoin delivery: PASS
maintenance counters correct: PASS
TAIL case: PASS
CORE case: PASS
unrelated branch stable: PASS
Gossip/native Structured/AHBN influence absent: PASS
metrics produced: PASS
Markdown terminal record complete: PASS
```

## K4.5 Result

```text
K4.5 FINAL STATUS: PASS
```

GKE cluster was NOT automatically torn down.
Use `scripts/teardown_gke.sh` manually when shutdown is required.
