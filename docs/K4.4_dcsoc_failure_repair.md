# K4.4 — DC-SoC CORE Failure → Repair

## Environment

- Date: 2026-08-23 (Asia/Kuala_Lumpur)
- Project root: `/Users/wwiras/Documents/src/AHBN_GKEProj/ahbn2_gke`
- Python interpreter: `/Users/wwiras/Documents/src/AHBN_GKEProj/venv_ahbn2/bin/python`
- Git commit: `a694f515e5163dd11fcafcf386e2cf10ff698f74`
- kubectl context: `gke_stoked-cosine-415611_us-central1-a_bcgossip-cluster`
- namespace: `ahbn-k4`
- active strategy: `dcsoc`
- failure mechanism: application-level fail-stop (`FailStop` RPC), then DC-SoC inactive broadcast
- failure mechanism source/reference: `app/controller.py:200-219,234-263,590-617`; active Exp10 `failure.mode: ch_failure`

## GKE prerequisite

Command:

```bash
kubectl config current-context
kubectl get nodes -o wide
kubectl get namespaces
kubectl get pods -A -o wide
```

Output (material lines; command output was also retained in the K4.4 terminal session):

```text
gke_stoked-cosine-415611_us-central1-a_bcgossip-cluster
7 nodes: STATUS=Ready, VERSION=v1.35.6-gke.1641000
ahbn-k4 Active
peer-0 ... peer-7: 1/1 Running, RESTARTS=0
```

An initial sandboxed invocation failed because gcloud could not write its credential database. It was preserved and rerun with approved access:

```text
ERROR: (gcloud.config.config-helper) Unable to create private file
[/Users/wwiras/.config/gcloud/credentials.db]: [Errno 1] Operation not permitted
Unable to connect to the server: getting credentials ... exit code 1
```

Conclusion: GKE prerequisite **PASS** after allowing the existing credential plugin to access its configuration. No cluster was created.

## Frozen failure mechanism verification

Implementation/configuration evidence:

```text
app/peer.proto: rpc FailStop(Empty) returns (Ack)
app/controller.py: fail_stop_peer() calls stub.FailStop(...)
app/controller.py: mode in ("node_failure", "ch_failure") calls fail_stop_peer,
then broadcast_dcsoc_maintenance(... available=False, reason=mode)
experiments/exp10.yaml: failure.mode: ch_failure
```

Conclusion:

```text
Authoritative later-experiment failure mechanism:
application-level FailStop RPC plus DC-SoC inactive-event broadcast.
It is not pod deletion; pod deletion belongs to churn.
```

## Preflight Kubernetes state

### Nodes

```text
Seven GKE nodes Ready.
```

### Pods

Command: `kubectl -n ahbn-k4 get pods -o wide`

```text
peer-0 through peer-7: Running, 1/1 Ready, 0 restarts
```

### Services

```text
ahbn-peer   ClusterIP None   50051/TCP
```

### Jobs

```text
No resources found in ahbn-k4 namespace.
```

## Healthy DC-SoC structure

### Roles

```text
MASTER/CORE: peer-0 (cluster 0)
CORE:        peer-4 (cluster 1)
TAIL:        peers 1,2,3 under peer-0; peers 5,6,7 under peer-4
```

### Structural relationships

```text
peer-0 -> peer-1, peer-2, peer-3, peer-4
peer-4 -> peer-5, peer-6, peer-7
```

Topology reports `dynamic_maintenance=false`; maintenance is event-driven and no periodic maintenance exists.

## Target CORE selection

```text
failed_core: peer-4
pod: peer-4
pod UID: 2a2ed13e-04cc-4767-a9a4-05b439688349
parent: MASTER peer-0
downstream TAILs: peer-5, peer-6, peer-7
replacement candidates: peer-5, peer-6, peer-7
expected replacement: peer-5
selection rule: active same-cluster member with highest original physical degree,
                then lowest peer ID
unrelated branch: peer-0 -> peer-1, peer-2, peer-3
```

All candidates had physical degree 3, so the frozen lowest-ID tie-break selected peer-5 before failure observation.

## Pre-failure maintenance state

```text
core_replacement_count: 0 (no maintenance events in any peer log)
recluster_count: 0
maintenance_event_count: 0
maintenance_active: false / no event in progress
```

## Baseline pre-failure dissemination

Message ID: `k4_4_pre_failure_20260823T1955`

Command/output:

```text
StartRun on peer-0: ok: true; message: "run started"
peer-0 forward dst_peer=4
peer-4 received_new hop=1 src_peer=0
peer-4 forward dst_peer=5
peer-4 forward dst_peer=6
peer-4 forward dst_peer=7
peers 5,6,7 received_new hop=2 src_peer=4
```

Observed path: `MASTER peer-0 -> CORE peer-4 -> TAIL peers 5,6,7`

Result: **PASS**

## Failure injection

An initial command at `2026-08-23T11:48:42Z` used the wrong stub attribute and failed before changing state:

```text
AttributeError: 'PeerServiceStub' object has no attribute 'Fail'
command terminated with exit code 1
```

The immediate probe returned `ok: true; message: "run started"`; therefore no failure occurred and no inactive broadcast ran.

Actual injection timestamp: `2026-08-23T11:49:23.250844Z`

Command semantics:

```text
peer-4 FailStop(Empty)
ApplyDCSOCMaintenance(node_id=4, available=false,
                      explicit_du=false, reason="ch_failure")
to surviving peers 0,1,2,3,5,6,7
```

Output:

```text
FAILSTOP ok: true
message: "peer entered fail-stop state"
MAINT 0,1,2,3,5,6,7: ok: true; message: "maintenance applied"
```

## Kubernetes unavailability evidence

```text
Before: peer-4 UID 2a2ed13e-04cc-4767-a9a4-05b439688349,
        phase=Running ready=true restarts=0
After:  same UID, phase=Running ready=true restarts=0
GetStatus remained reachable, but StartRun returned message: "peer failed".
peer-4 logged event=peer_failed at ts=1787485765.8837764.
```

The frozen fail-stop disables dissemination while leaving the container and gRPC server alive. Genuine peer unavailability: **PASS**. Pod termination/recreation: not part of this mechanism.

## DC-SoC inactive-CORE detection

```text
failed_core: peer-4
detection timestamp: first surviving replica maintenance_start=1787485765.960373
evidence: every surviving replica logged failed_node=4, failed_role=core,
          maintenance_action=leave, maintenance_reason=ch_failure
```

## Inactive relationship removal

Before: `peer-0 -> peer-4 -> peers 5,6,7`

After: `peer-0 -> peer-5 -> peers 6,7`; peer-4 absent from post-repair forwarding.

Result: **PASS**

## Maintenance start

Representative authoritative replica (`peer-0`):

```text
maintenance_start: 1787485765.960373 seconds since Unix epoch
```

## Deterministic replacement selection

```text
expected replacement: peer-5
observed replacement: peer-5
selection rule: maximum original physical degree, lowest ID tie-break,
                active member of affected retained cluster
result: PASS
```

## CORE replacement counter

```text
before: 0
after: 1
delta: +1
result: PASS
```

All seven surviving replicas reported the same value.

## Branch repair

### Before

```text
peer-0 -> peer-4
peer-4 -> peer-5, peer-6, peer-7
```

### After

```text
peer-0 -> peer-5
peer-5 -> peer-6, peer-7
peer-4 inactive and removed from active path
```

### Result

**PASS**

## Unrelated branch comparison

Before: `peer-0 -> peer-1, peer-2, peer-3`

After: `peer-0 -> peer-1, peer-2, peer-3`

Result: **PASS**

## Reclustering check

```text
recluster_count before: 0
recluster_count after: 0
delta: 0
result: PASS
```

## Maintenance end

Representative replica (`peer-0`):

```text
maintenance_end: 1787485765.9604378 seconds since Unix epoch
```

## Maintenance duration

```text
maintenance_start: 1787485765.960373
maintenance_end: 1787485765.9604378
maintenance_duration: 0.00006303999998635845
unit: seconds
```

Replica durations ranged from approximately 0.0000533 to 0.0001203 seconds. These are instrumentation observations, not performance claims.

## Maintenance event count

```text
before: 0 per surviving process replica
after: 1 per surviving process replica
delta: +1 attributable event per replica
```

## Post-repair dissemination

Message ID: `k4_4_post_repair_20260823T1950`

Command/output:

```text
StartRun on peer-0: ok: true; message: "run started"
peer-0 forward dst_peer=1,2,3,5
peer-5 received_new hop=1 src_peer=0 is_cluster_head=true
peer-5 forward dst_peer=6,7
peers 6,7 received_new hop=2 src_peer=5
peer-4: no event for this message
```

Observed repaired paths:

```text
MASTER peer-0 -> replacement CORE peer-5 -> TAIL peer-6
MASTER peer-0 -> replacement CORE peer-5 -> TAIL peer-7
```

Result: **PASS**

All repaired obligations were honored; peer-5 did not forward to itself. The `fanout=3` metadata did not cap peer-0's four DC-SoC obligations.

## Kubernetes recovery

```text
initial pod state: Running / Ready
failure state: pod remained Running / Ready; logical peer fail-stop active
recovery/recreation state: none (not part of frozen Exp10 fail-stop)
old pod UID: 2a2ed13e-04cc-4767-a9a4-05b439688349
new pod UID: N/A; unchanged
```

## Kubernetes vs DC-SoC timeline

```text
T0  peer-4 pod Running/Ready; baseline dissemination succeeds
T1  1787485765.8837764 peer-4 enters fail-stop
T2  StartRun to peer-4 returns "peer failed"; pod remains Running/Ready
T3  1787485765.960373 peer-0 replica maintenance_start/detects inactive CORE
T4  peer-5 selected; inactive edge removed; affected branch repaired
T5  1787485765.9604378 peer-0 replica maintenance_end
T6  1787485797.6741872 post-repair message injected
T7  peer-0 -> peer-5 -> peers 6,7 succeeds
T8  Kubernetes pod still same UID/Running/Ready; no recreation expected
```

## Isolation

### Gossip

The strategy dispatch was `strategy=dcsoc`; the repair was executed only by `DCSOCMaintenance`. No random sampling selected repair or forwarding targets. The generic log field `mode="gossip"` is stale/non-decisional metadata; observed forwarding exactly matched all DC-SoC structural obligations (including four from peer-0 despite `fanout=3`).

```text
Gossip decision leakage: NONE
```

### Native Structured

No `cluster_targets` or native Structured repair entry point was invoked. The branch used `dcsoc_parent`/`dcsoc_children` updated by `DCSOCMaintenance`.

```text
native Structured decision leakage: NONE
```

### AHBN

Standalone DC-SoC creates no AHBN controller for decisions, and maintenance broadcasts return unless `strategy=dcsoc`. No AHBN update, score, weight, adaptive mode, or adaptive fanout influenced the repair or observed forwarding.

```text
AHBN behavioural influence during K4.4 = 0
```

## Final dissemination metrics

```text
delivery: peers 0,1,2,3,5,6,7 received; failed peer-4 correctly excluded
propagation delay: present as latency_ms on every received_new event
duplicates: no duplicate received event observed for the validation message
total successful forwards: 6 (0->1,2,3,5 and 5->6,7)
```

## Post-test Kubernetes state

Command:

```bash
kubectl -n ahbn-k4 get pods -o wide
kubectl -n ahbn-k4 get jobs
```

Output:

```text
peer-0 through peer-7: 1/1 Running, RESTARTS=0
peer-4 UID unchanged, phase=Running, ready=true, restarts=0
No resources found in ahbn-k4 namespace.
```

## Local frozen-semantics regression verification

Command:

```bash
PYTHONPYCACHEPREFIX=/tmp/k4_4_pycache \
  /Users/wwiras/Documents/src/AHBN_GKEProj/venv_ahbn2/bin/python \
  -m unittest -v tests.test_k3_5_dcsoc_dynamic tests.test_k3_4_dcsoc_static
```

Output:

```text
Ran 33 tests in 0.012s

OK
```

This verifies the frozen replacement, local-repair, no-recluster, isolation,
unbudgeted-obligation, and sender/self rules remained valid after K4.4. No
production code was changed.

## K4.4 PASS/FAIL checklist

```text
GKE prerequisite: PASS
authoritative failure mechanism established: PASS
healthy DC-SoC structure before failure: PASS
target CORE genuinely unavailable: PASS
unavailability observable: PASS
inactive CORE detected: PASS
inactive CORE removed logically: PASS
replacement selected deterministically: PASS
core_replacement_count delta correct: PASS
affected branch repaired: PASS
unrelated branches unchanged: PASS
recluster_count unchanged as intended: PASS
maintenance_start present: PASS
maintenance_end present: PASS
maintenance_duration present: PASS
post-repair dissemination resumes: PASS
Kubernetes recovery separately observed: PASS (no recreation under frozen mechanism)
Gossip influence absent: PASS
native Structured influence absent: PASS
AHBN influence absent: PASS
metrics produced: PASS
Markdown terminal record complete: PASS
```

## K4.4 Result

```text
K4.4 PASS
```

## Notes / anomalies

```text
1. Initial sandboxed kubectl attempt failed on gcloud credential-file permissions;
   approved credential access resolved it without changing the cluster.
2. The first failure command used non-existent stub method `Fail`; it failed
   before mutation. A successful immediate StartRun probe proved no failure occurred.
3. The runtime generic `mode` log field says `gossip` even when strategy=dcsoc.
   Decision-path and obligation evidence proves no Gossip sampling was used.
4. Frozen Exp10 fail-stop is logical/process-state failure, so Kubernetes does
   not terminate or recreate the pod. This is documented separately rather than
   misreported as Kubernetes workload recovery.
```

```text
============================================================
K4.4 — DC-SOC CORE FAILURE → REPAIR
============================================================
GKE prerequisite:                         PASS
Frozen failure mechanism established:     PASS
DC-SoC structure healthy before failure:  PASS

Failed CORE: peer-4
Expected replacement: peer-5
Observed replacement: peer-5

Target CORE genuinely unavailable:        PASS
Unavailability observable:                PASS
Inactive CORE detected:                   PASS
Inactive relationship removed:            PASS
Deterministic replacement:                PASS
Affected branch repaired:                 PASS
Unrelated branches unchanged:             PASS

core_replacement_count: before=0 after=1 delta=+1
recluster_count: before=0 after=0 delta=0

maintenance_start:    1787485765.960373
maintenance_end:      1787485765.9604378
maintenance_duration: 0.00006303999998635845 seconds

Post-repair dissemination:                PASS
Observed repaired path: peer-0 -> peer-5 -> peers 6,7
Kubernetes recovery separately observed:  PASS (none expected)
Gossip decision leakage:                  NONE
Native Structured leakage:                NONE
AHBN influence:                           NONE
Final metrics emitted:                    PASS
Markdown terminal record:                 PASS

K4.4 FINAL STATUS: PASS
============================================================
```

GKE cluster was NOT automatically torn down.
Use `scripts/teardown_gke.sh` manually when shutdown is required.
