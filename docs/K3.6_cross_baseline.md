# K3.6 — Cross-Baseline Isolation + Final Regression Gate

## A. Verdict

```text
K3.6 FINAL STATUS: PASS
K3 BASELINE RECONCILIATION: PASS / FROZEN
K4 READINESS: YES
K4 EXECUTED: NO
```

K3.6 performed validation, regression, and isolation only. Production
algorithm modifications: NONE. Parameter tuning: NONE. ControlSim changes:
NONE. Production or distributed campaigns: NONE.

## B. Scope

The common forwarding boundary, state boundary, controller boundary, fanout
boundary, dynamic-response boundary, deterministic replay, and static build
surface were tested for AHBN, standalone Gossip, standalone Structured, and
DC-SoC. K4 and all scientific campaigns were explicitly excluded.

## C. Environment / provenance

```console
$ pwd
/Users/wwiras/Documents/src/AHBN_GKEProj/ahbn2_gke
$ git rev-parse --show-toplevel
/Users/wwiras/Documents/src/AHBN_GKEProj/ahbn2_gke
$ git rev-parse HEAD
a57e5e7a53104b6293294fc396510fbc949323ac
$ git status --short
$ git diff --check
$ /Users/wwiras/Documents/src/AHBN_GKEProj/venv_ahbn2/bin/python --version
Python 3.14.6
```

The pre-K3.6 worktree was clean. No reset, clean, stash, or discard was used.

## D. Frozen K3.1–K3.5 references

- K3.1 freezes canonical AHBN observations, controller, fanout `[2,4]`, and
  dispatch against ControlSim v0.61.
- K3.2 freezes standalone Gossip as eligible physical-neighbor selection with
  peer-local seeded sampling, optional independent fanout, and no controller.
- K3.3 freezes standalone Structured as fixed role obligations without an AHBN
  cap, random sampling, or self-healing.
- K3.4 freezes density-derived DC-SoC MASTER/CORE/TAIL structure and unbudgeted
  structural forwarding.
- K3.5 freezes immediate event-driven leave/failure repair, rejoin, and
  explicit `du`; it freezes no implicit recurring maintenance interval.

The K3.5 document contains its historical blocked checkpoint and the later
authorized resolution and final PASS. Its corrected final gate closes B06.

## E. Cross-baseline semantic matrix

| Algorithm | Target selection | Fanout | Structural state | Dynamic maintenance | AHBN controller |
|---|---|---|---|---|---|
| AHBN | adaptive gossip/cluster mode | canonical 2..4 | AHBN cluster roles where selected | none | yes |
| Gossip | eligible physical neighbors | standalone optional/static rule | ignored | none | no |
| Structured | fixed head/member/gateway obligations | no AHBN cap | fixed | none | no |
| DC-SoC | MASTER/CORE/TAIL obligations | no generic truncation | density-derived | topology transitions and explicit `du` | no |

## F. Common deterministic topology

The X01–X30 fixture gives every comparator an independent copy of the same
physical neighbor list, peer ID, sender, availability set, structural metadata,
and frozen seed. The Structured head has six eligible obligations; the DC-SoC
CORE has seven configured children (six after sender exclusion); standalone
Gossip has fanout five; AHBN begins at fanout two.

## G. AHBN isolation

K1/K2 controller and dispatch parity passed. AHBN alone executes adaptive
update, retains fanout `[2,4]`, and ignores DC-SoC children, counters, and
recluster state. K2 trace recomputation validates raw observations, EWMA,
score, sigmoid, mode, fanout, and dispatch against the frozen reference.

## H. Gossip isolation

Gossip selected only eligible physical neighbors, excluded sender/self, used
peer-local seeded sampling without replacement, honored fanout five, ignored
all supplied structural metadata, and instantiated no AHBN controller/state.

## I. Structured isolation

Structured preserved all six eligible native obligations. It used neither
Gossip sampling nor AHBN fanout truncation and performed no DC-SoC repair.

## J. DC-SoC isolation

DC-SoC preserved density-derived roles and unbudgeted structural obligations.
Its seven configured children were not limited by the legacy fanout three or
AHBN bounds. Static and dynamic ControlSim-derived suites passed.

## K. Sender/self invariant

X15/X16 and the comparator-specific suites passed for all four algorithms.

## L. Duplicate invariant

X17 delivered the same already-seen message to each comparator. Every path
counted/returned duplicate before target selection; no refanout occurred.

## M. Controller isolation

K1/K2 prove canonical AHBN controller execution. Standalone initialization
tests and X03–X11/X19 prove Gossip, Structured, and DC-SoC have no AHBN
controller/state and do not call `adaptive_update` for target decisions.

## N. Fanout isolation

Adversarial fanouts produced AHBN=2, Gossip=5, Structured=6, and DC-SoC=6
eligible targets. Changing AHBN to four did not alter deterministic Structured
or DC-SoC results; reset-seed Gossip remained governed by its own fanout.

## O. Mutable-state isolation

Independent peer lists, RNGs, AHBN controller states (K2), and DC-SoC
maintenance replicas were exercised. A CORE failure/rejoin in one replica did
not mutate the second replica or any other strategy.

## P. Failure cross-baseline fixture

Gossip and Structured availability filtering remains non-reconstructive; AHBN
failure information remains an observation rather than topology repair;
DC-SoC alone performed local deterministic CORE replacement. No performance
comparison was made.

## Q. Churn cross-baseline fixture

Only DC-SoC detached/repaired/reinstated structural state. Gossip used current
eligible neighbors, Structured exposed fixed relationship availability, and
AHBN retained its canonical churn observation/controller route.

## R. Slow/overload fixture

Increasing delay/overload while ACTIVE caused no DC-SoC transition, detach,
replacement, recluster, generation change, or counter increment. Standalone
target semantics stayed fixed; AHBN's canonical observation path remained the
only adaptive route. `SLOW != FAILED`: PASS.

## S. Explicit DC-SoC du isolation

Explicit `du` incremented only the DC-SoC recluster/generation state. AHBN,
Gossip, and Structured object state and RNG state remained byte-for-byte/logical
equivalent.

## T. Metrics observationality

Changing duplicate, receive, and latency metric fields did not change seeded
Gossip targets. K2 proves AHBN consumes only its canonical observations, and
K3.5 proves maintenance timing/counters are recorded after decisions and are
not decision inputs.

## U. Experiment-identity isolation

Static search found experiment/run identifiers in configuration generation,
environment perturbation selection, logging, plotting, and result labels.
X27 assigned different experiment identities to otherwise identical strategy
instances and obtained identical seeded targets. No experiment identity branch
was found in comparator target selection or controller equations.

## V. Strategy execution-order isolation

AHBN→Gossip→Structured→DC-SoC and the reverse order produced identical
per-algorithm deterministic results when each algorithm's frozen seed semantics
were reset. Shared input topology was not mutated.

## W. RNG isolation

Standalone Gossip uses peer-local `random.Random(topology_seed)`. Canonical
AHBN retains the K2-frozen seeded process RNG. Structured/DC-SoC consume
neither stream during deterministic selection. Gossip alone and after both
structural strategies selected identical targets.

## X. Focused K3.6 tests

`tests/test_k3_6_cross_baseline.py` contains 12 executable methods covering the
required X01–X30 logical catalog. Two initial harness runs are retained below:
the first omitted manually constructed AHBN runtime fields and compared copied
RNG objects by identity; the second failed to reset AHBN's K2-frozen process
seed. No production defect was indicated and no production file was changed.

Final focused portion in the combined suite: 12/12 methods PASS.

## Y. Full K1/K2/K3 regression

```text
K1:   9 methods PASS
K2:  21 methods PASS
K3.2: 8 methods PASS
K3.3: 9 methods PASS
K3.4:16 methods PASS
K3.5:16 methods PASS
K3.6:12 methods PASS
TOTAL: 92 methods PASS
```

K3.1 has no dedicated module; its executable AHBN gate is K1+K2, both rerun
here. All frozen ControlSim parity fixtures embedded in K2/K3.2/K3.3/K3.4 and
the K3.5 dynamic foundation suite passed.

## Z. Static validation

```text
Python compilation: PASS
Source YAML parsing: PASS (15 files)
Shell syntax: PASS
Helm lint: PASS (1 chart, 0 failed; icon recommendation only)
Helm render: PASS
Rendered YAML parsing: PASS (7 documents)
git diff --check: PASS
```

No Helm deployment was performed.

## AA. Final K3 semantic manifest

POST-K3 FROZEN KUBERNETES SEMANTIC MANIFEST:

```text
5ee9314b58a235144287f7fe11dad0943c4a68abb952ef340819e97a6064f6df  app/ahbn_controller.py
b8bfb46817a6cdb10cb9eb053c05dea77001552667707704845ca9140f28f457  app/observations.py
aa7c1323ea39591f4d512531d0af4aa0c8556f977e9b2f4f8bd4d2db34b77cd6  app/peer.py
4636869fa83fed3673bc76f92122722f3e50ddccc0e43624ef60a6d2ebb56d8c  app/dcsoc_maintenance.py
6683bcdccc4c6460033751a434b385412bb9be10b8862425c26494af3b1fa2ea  app/controller.py
0d5e9da2569f22d2ee87f2a6df0293781661870f2ebb4f6159a62a1c7e0bf7d3  app/gen_topology.py
4c56857d45cffb9691db7b4b0574197b38f1afb92467d2e608ecfb912aa19f7a  app/peer.proto
fd9698f9f51996c4acaf994b91d87baac183c016c0ecc9b0ae396ef3adc29909  app/Dockerfile
3b0124c5750053c1b1f61c520438e04092b2a4e2dd02d3251a130043bd10e2b4  helm/ahbn/values.yaml
34c014b99243e74fc3c764d108a117b09e3b150789fd14280b4ede164afc3422  helm/ahbn/topology.json
a4bbdec7d104d765ec9d1e2d2154326a738015edb4582ff653e5fb306628cd7d  helm/ahbn/templates/configmap.yaml
cd9827559b9609a2e870787220a6e5d600919f066b44e0faf6025701edfb14bf  helm/ahbn/templates/statefulset.yaml
```

## AB. Final diff audit

K3.6 owns only this audit and `tests/test_k3_6_cross_baseline.py`. Production
algorithm/configuration diff: NONE. The final status/diff/check output is
recorded in section AE. No experiment tuning, legacy override, parameter drift,
or algorithm mixing was found. No pre-existing whitespace warning was present
at the K3.6 start or final check.

## AC. K3 freeze declaration

```text
K3 FINAL FREEZE

AHBN: canonical / frozen
Gossip: reconciled / frozen
Structured: reconciled / frozen
DC-SoC-inspired: static / frozen; dynamic maintenance / frozen
Cross-baseline isolation: verified
Full regression: verified
```

KUBERNETES ALGORITHM FREEZE RULE: after K3.6 there is no AHBN, Gossip,
Structured, or DC-SoC algorithm modification; no parameter tuning; and no
experiment-specific algorithm branch unless a genuine implementation defect
against the frozen K1/K2/K3 reference is demonstrated. Future stages may
configure perturbations, orchestration, seeds, measurement, and timing without
redesigning comparator semantics.

## AD. Remaining blockers

```text
B06 — DC-SoC static/dynamic implementation: CLOSED
B09 — standalone Gossip reconciliation: CLOSED
Remaining unchanged: B05, B07, B08, B11, B12
```

K3.6 does not claim to resolve the remaining experiment, orchestration,
distributed seeding, or final metric/recovery blockers.

## AE. Exact commands and terminal output

Commands are chronological. Large read-only source/audit inspections are
identified with their returned line/match summaries; decisive executable
outputs are preserved exactly or with every named method represented by the
recorded count.

```console
$ sed -n '1,240p' /Users/wwiras/.codex/attachments/31f65d51-edad-4cef-a078-6922e8cf5ab9/pasted-text.txt
[K3.6 request lines 1-240]
$ sed -n '241,520p' .../pasted-text.txt
[K3.6 request lines 241-520]
$ sed -n '521,900p' .../pasted-text.txt
[K3.6 request lines 521-900]
$ sed -n '901,1200p' .../pasted-text.txt
[K3.6 request lines 901-1200]
$ sed -n '1201,1500p' .../pasted-text.txt
[K3.6 request lines 1201-1500]
$ sed -n '1501,1650p' .../pasted-text.txt
[K3.6 request lines 1501-end]

$ pwd; rg --files -g 'AGENTS.md' ...; git rev-parse --show-toplevel; git rev-parse HEAD; git status --short; git diff --check; .../bin/python --version; rg --files docs tests | sort
/Users/wwiras/Documents/src/AHBN_GKEProj/ahbn2_gke
/Users/wwiras/Documents/src/AHBN_GKEProj/ahbn2_gke
a57e5e7a53104b6293294fc396510fbc949323ac
Python 3.14.6
[nine K3/K1/K2 documents and six pre-existing focused test modules listed]

$ sed -n ... docs/K3.1_ahbn.md docs/K3.2_gossip.md docs/K3.3_structured.md docs/K3.4_dcsoc_static.md docs/K3.5_dcsoc_dynamic.md
[1,445 audit lines read; terminal reported 15,888 tokens and displayed the frozen declarations, including K3.5's authorized PASS resume]

$ rg -n '^class |^    def test_' tests/test_k*.py; rg -n ... app/*.py; sed -n ... tests/test_k3_5_dcsoc_dynamic.py tests/test_k3_4_dcsoc_static.py
[existing class/method catalog and production call paths inspected]

$ PYTHONPYCACHEPREFIX=/tmp/k3_6_pycache .../bin/python -m unittest -v tests.test_k3_6_cross_baseline
Ran 11 tests in 0.007s
FAILED (failures=1, errors=5)
[fixture errors: missing `failed` on manually constructed AHBN peers; copied RNG identity comparison]

$ PYTHONPYCACHEPREFIX=/tmp/k3_6_pycache .../bin/python -m unittest -v tests.test_k3_6_cross_baseline
Ran 11 tests in 0.006s
FAILED (failures=2)
[fixture failures: AHBN process RNG was not reset between identity/order comparisons]

$ sed -n '380,490p' app/peer.py; sed -n '240,320p' tests/test_k2.py; sed -n '1,140p' tests/test_k3_2_gossip.py
[confirmed K2-frozen AHBN process RNG and standalone Gossip peer-local RNG]

$ PYTHONPYCACHEPREFIX=/tmp/k3_6_pycache .../bin/python -m unittest -v tests.test_k3_6_cross_baseline
Ran 11 tests in 0.004s
OK

[X17 duplicate-all-algorithms fixture added; no production change]

$ PYTHONPYCACHEPREFIX=/tmp/k3_6_pycache .../bin/python -m unittest -v tests.test_k1 tests.test_k2 tests.test_k3_2_gossip tests.test_k3_3_structured tests.test_k3_4_dcsoc_static tests.test_k3_5_dcsoc_dynamic tests.test_k3_6_cross_baseline
[all 92 named methods reported ok]
Ran 92 tests in 0.050s
OK

$ PYTHONPYCACHEPREFIX=/tmp/k3_6_pycache .../bin/python -m compileall -q app tests
[exit 0]
$ .../bin/python -c '... yaml.safe_load ...'
Source YAML parsing: PASS (15 files)
$ find . -type f -name '*.sh' -not -path './.git/*' -print0 | xargs -0 -n1 bash -n
[exit 0]
$ helm lint helm/ahbn
==> Linting helm/ahbn
[INFO] Chart.yaml: icon is recommended

1 chart(s) linted, 0 chart(s) failed
$ git diff --check
[exit 0]
$ helm template k36 helm/ahbn --namespace ahbn > /tmp/k3_6_rendered.yaml
[exit 0]
$ .../bin/python -c 'import yaml; docs=list(yaml.safe_load_all(open("/tmp/k3_6_rendered.yaml"))); print(...)'
Rendered YAML parsing: PASS (7 documents)

$ rg -n -i 'Exp08|Exp10|Exp11|Exp12|experiment|scenario|run_id' app -g '*.py'
[matches limited to generation/configuration, perturbation targeting, logging, plotting, and labels; no comparator decision branch]
$ rg -n 'B05|B06|B07|B08|B09|B11|B12' docs -g '*.md'
[K3.2 B09 CLOSED; K3.5 corrected final gate B06 CLOSED; original register retains B05/B07/B08/B11/B12]
$ shasum -a 256 [12 production semantic files]
[hashes reproduced in section AA]
$ git status --short
?? tests/test_k3_6_cross_baseline.py
$ git diff --stat
$ git diff -- tests/test_k3_6_cross_baseline.py
[untracked files are not included by git diff]

$ git status --short; git diff --check; git diff --stat; git diff; shasum -a 256 tests/test_k3_6_cross_baseline.py docs/K3.6_cross_baseline.md
?? docs/K3.6_cross_baseline.md
?? tests/test_k3_6_cross_baseline.py
10c22ead8d146f2d66d301a1ef686b69efd9fec869cc4b27d45a631bd67328f0  tests/test_k3_6_cross_baseline.py
dea570ab9c11f26cd2b1230f82254abb55d3c97f04255b9b8a88477592c0756a  docs/K3.6_cross_baseline.md

`git diff --check`, `git diff --stat`, and `git diff` produced no output
because the two K3.6-owned files are untracked. Their complete contents were
audited directly; no tracked production file changed.
```

Final terminal summary:

```text
K3.6 FINAL STATUS: PASS

Cross-baseline isolation:
- AHBN identity: PASS
- Gossip identity: PASS
- Structured identity: PASS
- DC-SoC identity: PASS
- controller leakage: NONE / PASS
- fanout leakage: NONE / PASS
- state leakage: NONE / PASS
- maintenance leakage: NONE / PASS

Common invariants:
- sender exclusion: PASS
- self exclusion: PASS
- duplicate no-refanout: PASS

Dynamic fixtures:
- failure isolation: PASS
- churn isolation: PASS
- slow/overload isolation: PASS
- explicit DC-SoC du isolation: PASS

Determinism:
- replay: PASS
- strategy-order independence: PASS
- RNG isolation: PASS

Regression:
- K1: PASS
- K2: PASS
- K3.2: PASS
- K3.3: PASS
- K3.4: PASS
- K3.5: PASS
- K3.6 focused tests: PASS

Static:
- Python: PASS
- YAML: PASS
- Helm lint: PASS
- Helm render: PASS
- rendered YAML: PASS
- shell: PASS
- git diff --check: PASS

Freeze:
- final K3 manifest written: YES
- algorithm modification during K3.6: NO
- parameter tuning during K3.6: NO
- ControlSim modified: NO
- production campaign executed: NO

Blockers:
- B06: CLOSED
- B09: CLOSED
- remaining: B05, B07, B08, B11, B12

Documentation:
- docs/K3.6_cross_baseline.md updated: YES

K3 BASELINE RECONCILIATION: PASS / FROZEN
K4 READINESS: YES
K4 EXECUTED: NO
```

K3 IS COMPLETE AND THE KUBERNETES ALGORITHM IMPLEMENTATION IS FROZEN.
